- project: seamless-test
- execution: remote

- stage jobserver-test:
    - execution: remote
    - remote: jobserver

- stage database:
    - execution: process

- stage simple-remote:
    - execution: remote
    - remote: daskserver

- stage spawn-config:
    - execution: spawn

- stage spawn-persistent:
    - execution: spawn

- stage persistent-test:
    - execution: process
    - persistent: true

- stage fingertip:
    - execution: process
    - persistent: true
