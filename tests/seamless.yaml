- project: myproject
- execution: process

- stage jobserver-test:
    - execution: remote
    - remote: jobserver

- stage simple-remote:
    - execution: remote
    - remote: daskserver

- stage spawn-config:
    - execution: spawn
