- project: myproject
- execution: remote

- stage jobserver-test:
    - execution: remote
    - remote: jobserver

- stage simple-remote:
    - execution: remote
    - remote: daskserver

- stage spawn-config:
    - execution: spawn

- stage spawn-persistent:
    - execution: spawn

- stage persistent:
    - execution: process
